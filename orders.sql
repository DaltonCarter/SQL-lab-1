-- CREATE TABLE orders (
--     order_id SERIAL PRIMARY KEY, 
--     person_id SERIAL,
--     product_name VARCHAR (40),
--     product_price FLOAT,
--     quantity INTEGER
--     );

-- INSERT INTO orders (product_name, product_price, quantity)
-- VALUES ('root beer float', 8.99, 1), ('bacon burger', 13.50, 2),
-- ('chefs salad', 7.00, 1), ('kids meal', 10.50, 1),
-- ('heart attack special', 25.99, 1);

-- SELECT * FROM orders;

-- SELECT SUM(product_price) FROM orders;

--SELECT SUM(product_price) FROM orders
-- WHERE person_id = 5;

